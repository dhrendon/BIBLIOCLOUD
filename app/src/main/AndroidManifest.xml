<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bibliocloud">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BiblioCloud"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBTzm4kVIIjfgJoxYrOZmKze8vhLtpZhR4"/>

        <!-- Google Play Services Version -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <!-- ========================================= -->
        <!-- ACTIVIDAD PRINCIPAL -->
        <!-- ========================================= -->

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.BiblioCloud.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Registro -->
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/Theme.BiblioCloud.NoActionBar"
            android:exported="false"/>

        <!-- Home Usuario -->
        <activity android:name=".MainActivity" android:exported="false"/>

        <!-- ========================================= -->
        <!-- ACTIVIDADES DE ADMIN -->
        <!-- ========================================= -->

        <activity android:name=".AdminDashboardActivity" android:exported="false"/>
        <activity android:name=".UserManagementActivity" android:exported="false"/>

        <!-- 🆕 AGREGAR AQUÍ UserEditActivity -->
        <activity
            android:name=".UserEditActivity"
            android:exported="false"
            android:label="Editar Usuario"/>

        <activity android:name=".BookManagementActivity" android:exported="false"/>
        <activity android:name=".LoanManagementActivity" android:exported="false"/>
        <activity android:name=".SuggestionsManagementActivity" android:exported="false"/>
        <activity android:name=".BranchManagementActivity" android:exported="false"/>
        <activity android:name=".BranchLocationPickerActivity" android:exported="false"/>
        <activity android:name=".AdminPurchasesActivity" android:exported="false"/>

        <!-- ========================================= -->
        <!-- Actividades de Cajero -->
        <!-- ========================================= -->

        <activity android:name=".CashierDashboardActivity" android:exported="false"/>
        <activity android:name=".CashierOrdersActivity" android:exported="false"/>
        <activity android:name=".CashierInventoryActivity" android:exported="false"/>

        <!-- ========================================= -->
        <!-- Usuario general -->
        <!-- ========================================= -->

        <activity android:name=".SearchActivity" android:exported="false"/>
        <activity android:name=".FavoritesActivity" android:exported="false"/>
        <activity android:name=".ProfileActivity" android:exported="false"/>
        <activity android:name=".SuggestionsActivity" android:exported="false"/>
        <activity android:name=".MapActivity" android:exported="false"/>
        <activity android:name=".BibliotecaDetailActivity" android:exported="false"/>
        <activity android:name=".BranchBooksCatalogActivity" android:exported="false"/>
        <activity android:name=".PurchaseActivity" android:exported="false"/>
        <activity android:name=".MyPurchasesActivity" android:exported="false"/>
        <activity android:name=".BranchBooksActivity" android:exported="false"/>

        <!-- ========================================= -->
        <!-- Servicios -->
        <!-- ========================================= -->

        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

    </application>

</manifest>
